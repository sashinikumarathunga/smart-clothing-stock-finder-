// API base URL. When deployed under XAMPP/Apache, frontend and backend share
// the same origin (http://localhost), so this points at the backend public dir.
const API_BASE_URL = 'http://localhost:8080/smart-stock/backend/public';
